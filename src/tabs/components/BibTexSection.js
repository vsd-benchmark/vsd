export default function BibTex() {
    return (
      <section id="bibtx" className="container bibtex">
        <h4>BibTeX</h4>
        <code className="qoute_code">Here we can put the DOI qoutation code</code>
      </section>
    );
}